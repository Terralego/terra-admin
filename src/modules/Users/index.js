import React from 'react';

import config from './config';

import './styles.scss';

// const { path } = config;

export const Users = () => (
  <p>Users</p>
);

Users.config = config;

export default Users;
